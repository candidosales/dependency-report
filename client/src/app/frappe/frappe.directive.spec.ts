import { FrappeDirective } from './frappe.directive';

describe('FrappeDirective', () => {
  it('should create an instance', () => {
    const directive = new FrappeDirective();
    expect(directive).toBeTruthy();
  });
});
